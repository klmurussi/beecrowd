#include <iostream>

int main () {
    int A, B, C, D, diferenca;
	std::cin >> A >> B >> C >> D;
    diferenca = A * B - C * D;
    std::cout << "DIFERENCA = " << diferenca << std::endl;
	return 0;
}
